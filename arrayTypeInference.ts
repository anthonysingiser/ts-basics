// Don't change this part:
let dogTup: [string, string, string, string] = ['dog', 'brown fur', 'curly tail', 'sad eyes'];

// Your code goes here:
//define a variable myArr with type string[],
//without using: Array, or []

const myArr = dogTup.concat('new string')

myArr[50]= 'not a dog'